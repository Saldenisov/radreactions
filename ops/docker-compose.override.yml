# Override file for different deployment scenarios
# Copy this to docker-compose.override.yml and customize

version: '3.8'

services:
  ocr-validator:
    # Uncomment and modify for custom data path
    # volumes:
    #   - "/path/to/your/data:/app/data"

    # Uncomment to use different port
    # ports:
    #   - "8502:8501"

    # Environment variables
    environment:
      - BASE_DIR=/app/data
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

    # For production deployment
    # restart: always

    # Resource limits (optional)
    # deploy:
    #   resources:
    #     limits:
    #       memory: 2G
    #       cpus: '1.0'
